name: Rerun workflow by comment
author: harupy
description: Rerun workflow by comment

inputs:
  token:
    description: "A GitHub Token"
    required: true
  pull_number:
    description: "A pull request number"
    required: true
  comment:
    description: "An issue comment"
    required: true

runs:
  using: "docker"
  image: "Dockerfile"
